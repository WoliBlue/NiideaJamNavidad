using UnityEngine;

public class MuroSiguienteCliente : MonoBehaviour
{
    // Cuando se va el cliente por la derecha y desaparece, 
    // aparece delante de un muro que dice si cliente satisfecho o no. 
    // cuando pasan unos segundos, el muro desaparece y el siguiente cliente ya habrá aparecido antes detrás.
    // solo aparece cuando ha terminado la interacción con el cliente y le has vendido el producto.
    // Si el cliente está satisfecho, el muro dirá "Cliente Satisfecho" y si no "Cliente Insatisfecho"


    
    /// <summary>
    ///  El muro de siguiente cliente que aparece cuando el cliente se va.
    /// </summary>
    void Start()
    {
        
    }


    void Update()
    {
        
    }
}
