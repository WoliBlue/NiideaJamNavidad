using UnityEngine;

public class Dialogo : MonoBehaviour
{
    // El dialogo es un componente que se añade a los objetos que tienen dialogo con el jugador.
    // Para las Personas (Persona.cs y PersonalidadPersona)
    // Segun la persona y su personalidad, el dialogo sera diferente.
    // SOLO AQUI se gestiona la lógica de los dialogos. De si son buenos, malos o neutros
    // Mira de PersonalidadPersona.cs para ver si la respuesta del jugador es buena, mala o neutra.
    // Segun eso, se suma o resta al DecisionFinal de PersonalidadPersona.cs

    // Tambien se usa para el dialogo del tutorial al principio del juego.
    void Start()
    {
        
    }


    void Update()
    {
        
    }
}
